const Score = require('../models/score');

// Get the top ten high scores
exports.getScores = (req, res) => {
    // pass
    // const query = Score.find()
    res.send("NOT IMPLEMENTED: getScores");
}

// Add a new score to the db
exports.putNewScore = (req, res) => {
    // pass
    // const newScore = req.params.value;
}